# Optional Docker Compose overrides.
# docker-compose.yml already has safe defaults, so this file is not required
# for a fresh clone. Copy to .env only when you need custom names.

COMPOSE_PROJECT_NAME=mppl
PROJECT_NAME=mppl
XDEBUG=false
